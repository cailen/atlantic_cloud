- name: Install iptables-services service (Centos)
  yum:
    name: iptables-services
    state: latest

- name: Copy iptables template (Centos)
  template:
    src: templates/iptables.j2
    dest: /etc/iptables

- name: Restore from /etc/iptables (Debian)
  raw: iptables-restore < /etc/iptables

- name: Start iptables (Centos) 
  systemd:
    name: iptables
    enabled: yes
    state: started